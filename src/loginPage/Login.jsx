/* Create a login form that POSTs to /login (your Express route).

On success, redirect to / (which loads the main React app).

Bonus: Use fetch with credentials: 'include' so sessions work with cookies */